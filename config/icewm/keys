# Launch a terminal
key "Ctrl+Alt+t" alacritty

# Launch a browser
key "Ctrl+Alt+b" firefox

# Take a screenshot
key "Print" maim ~/screenshot.png


# Move/Tile Window to Left Half
key "Super+Alt+Left" icesh -f restore sizeto 50% 100% left top

# Move/Tile Window to Right Half
key "Super+Alt+Right" icesh -f restore sizeto 50% 100% right top

# Move/Tile Window to Top Half
key "Super+Alt+Up" icesh -f restore sizeto 100% 50% left top

# Move/Tile Window to Bottom Half
key "Super+Alt+Down" icesh -f restore sizeto 100% 50% left bottom

# Maximize / Restore Toggle
key "Super+Alt+x" sh -c "if icesh -f -a getStates | grep -q Maximized; then icesh -f restore; else icesh -f maximize; fi"

# turn on rofi
key "Super+space" rofi -show drun

# Volume keys
key "XF86AudioRaiseVolume" sh -c "volume-notify up"
key "XF86AudioLowerVolume" sh -c "volume-notify down"
key "XF86AudioMute"        sh -c "volume-notify mute"

# Brightness keys
key "XF86MonBrightnessDown" sh -c "brightness-notify down"
key "XF86MonBrightnessUp"   sh -c "brightness-notify up"

# Opacity keys (with Dunst notifications)
key "Super+bracketright" sh -c "opacity-notify up"
key "Super+bracketleft" sh -c "opacity-notify down"

# Selection to Clipboard (Drag region with slop)
key "Print" exec sh -c "maim -s | xclip -selection clipboard -t image/png"

# Selection to File (Saved with timestamp)
key "Ctrl+Print" exec sh -c "mkdir -p ~/Pictures/Screenshots && maim -s ~/Pictures/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png"

# Full Screen to Clipboard
key "Alt+p" exec sh -c "maim | xclip -selection clipboard -t image/png"

# Full Screen to File
key "Shift+Print" exec sh -c "mkdir -p ~/Pictures/Screenshots && maim ~/Pictures/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png"
